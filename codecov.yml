coverage:
  precision: 1
  round: down
  range: "40...100"
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: auto
        threshold: 5%

fixes:
  - "src/::src/"
