# CI-only requirements: core dependencies without heavy ML packages.
# torch, sentence-transformers, peft, and hdbscan are imported lazily in
# src/utils/embedding_client.py and are not needed to run the test suite.

pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
requests>=2.31.0
pyyaml>=6.0
portalocker>=2.8.0
scipy>=1.11.0
scikit-learn>=1.3.0
networkx>=3.1
python-louvain>=0.16
matplotlib>=3.7.0
