# Core runtime deps required by tests (avoids needing two pip install calls)
-r requirements-ci.txt

# Testing-only tools
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-xdist>=3.0.0
responses>=0.23.0
freezegun>=1.2.0
psutil>=5.9.0
